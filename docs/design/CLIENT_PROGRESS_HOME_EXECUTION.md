# Выполнение утверждённого плана Home / Progress

Приоритет имеет точный запрос владельца в `CLIENT_PROGRESS_HOME_APPROVED_SCOPE.md`. Он заменяет прежнюю остановку после оценки или этапов 1–3. Разрешены все четыре задачи и обычный production rollout; пилот и подтверждения между этапами не нужны. Исходные acceptance cases сохранены без изменения в этом файле запроса.

| Задача | Статус | Проверка / выпуск |
|---|---|---|
| 1 / YAFIT-480: достоверный и свежий факт | Выпущено, закрыто | #827 aa635b3 + #828 178094d; CI 34282379453/34284904484; Vercel 6338105500/6338315979, DB 34284076753 и повторный Yandex rollout 34285336984 успешны; новый iOS установлен и запущен |
| 2 / YAFIT-481: карта и личный итог Home | Локально проверено, готово к CI | 1274 frontend / 364 API / 111 infra; 20 Darwin + 20 Linux visual, 14 WebKit, 1 published-client browser; обычный rollout после CI |
| 3 / YAFIT-482: пересборка Progress | Ожидает 481 | Полная карта ниже и раскрывается из Home с тем же срезом |
| 4 / YAFIT-483: центр результатов | Ожидает 482 | Старт без пользовательского пилота |

## Приёмка задачи 1

- Первая запись, изменение и PR: `workout-results.test.ts`, SQL 0046/0047; предшествующая история обязательна. Объём и fixed-weight reps имеют отдельные ключи.
- Две конкретные тренировки, включая один день: unit; ссылки и даты component. Повторный вход/порядок массива не меняют выбор.
- Правка/удаление/завершение: общий invalidateWorkoutResults и realtime; unit проверяет состояния кэша. Исходные данные не переписываются.
- Пустые/неподтверждённые/ошибочные записи: unit/component; новая карточка не зависит от готовности сводки. У сохранённого анализа отдельные даты и пояснение свежести.
- История после переименования остаётся по ref; чужой ref, иной тип ввода и другой спортсмен изолированы. Метаданные отсутствующего оборудования не додумываются.
- UI использует существующие `card`, `eyebrow`, `actions`, `link`, `secondary` и токены graphite. Primary остаётся за записью/продолжением тренировки. Проверяются 390/430, light/dark и затронутые trainer состояния.

## Ограничения проверки

Выпуск #827: канонический https://fit-drab.vercel.app отдаёт новый `index-DF1apf_4.js` с личным фактом; HTTP 200, экран входа без JS-ошибок и горизонтального переполнения. iOS собран из слитого `aa635b3`, установлен и запущен на iPhone 17. Авторизованные Home/Progress проверены на контролируемых локальных данных, реальные production-записи не изменялись. Yandex deployment автоматически вернул предыдущий API: его inline smoke ещё ожидал `isPrimaryPr=true` и `hasPr=true` для единственной synthetic тренировки. Исправление сохраняет все readiness-проверки и ожидает новый контракт `false`; оно не меняет API, миграции или fixture.

Пользовательский пилот и удержание не проводились и не являются блокером выпуска. Новые production-записи реальных пользователей не создаются для тестов. Backend PR-контракт сохраняет прежние виды рекордов; этот этап исправляет отсутствие предшествующей истории, а новый личный факт явно называет свою метрику.

## Дополнение владельца от 9 сентября

«И главное не забывай, что у нас прикручена ИИ, ее тоже нужно не сломать и чтобы она качественно работала».

Во всех четырёх этапах сохраняются ИИ-анализ, его доступность, валидация и голосовой/текстовый разбор. Проверка новой композиции включает успешный анализ, недоступность сервиса, неполные данные и старую сводку после правок. Автоматическая генерация не заменяется карточками фактов; факты и ИИ должны быть согласованы. Prompt/matching/сохранение разбора не меняются ради UI. При задаче 3 нужно сохранить доступ к подробному анализу и понятному обновлению устаревшей версии. Полный check этапа 1 включает существующие проверки summary contract/quality, защищённого client copy, parsing и fallback; live-оценку модели нельзя считать выполненной по этим тестам.

## Приёмка задачи 2

Новая карта использует исходный SVG/фигуры и общий loadBodyMap, не отдельную модель нагрузки. Компактные зоны имеют отдельные подписанные кнопки; дополнительные зоны раскрываются. История и сводка не переписываются. Полная карта перенесена вниз уже на этом этапе, чтобы переход с Home был законченным и работал без ИИ. ИИ-запросы, prompt, голосовой parser и сохранение тренировок не менялись. Browser fixture детальных записей ограничен теми же synthetic ID, что и история. Длинный снимок раскрытой карты делается после навигационной проверки с увеличенной высотой кадра; сами нажатия проверяются в штатном mobile viewport.

Проверки этапа 2: полный npm run check — 1274 frontend, 364 API и 111 infra tests, typecheck/lint/build успешны; functions 81,66%, branches 75,81%, пороги сохранены. Новая зависимость публичного progress index включает также существующий экран замеров: добавлена интеграционная проверка сохранности duplicate-date protection, versioned edit, истории и custom metrics. Darwin visual: 20 passed; Linux: 19 passed, один crash процесса браузера, отдельный повтор 1 passed. Изображения Home light/dark, полной карты и затронутых trainer карточек просмотрены.

WebKit: 6 сценариев сохранения/замеров/ИИ и узких экранов прошли; 8 сценариев карты на 320/375/390/430 также прошли после обновления ожиданий раскрываемого контейнера. Проверки рамки перенесены на внешний disclosure, ограничения фигуры/кнопок/жестов не ослаблены.

Полный published-client browser-сценарий прошёл: сохранённые ИИ-разделы, личная настройка фигуры, новый порядок и открытие подробностей доступны. Stage 2 не оценивал реальный ответ модели: ограниченный live AI smoke предусмотрен после пересборки Progress.

CI #829 (34288940078): app/обе DB/WebKit прошли; один устойчивый visual fail замеров — сдвиг прокрутки на 12 px после перестановки карты. Исправлен только harness: фиксированное начало кадра, для базового Progress — стабильные workout/summary fixtures и верх страницы вместо сохранённой позиции у карты. Осмотрены expected/actual CI и новые кадры. Точные затронутые сценарии после исправления: 8 Linux + 8 Darwin passed; пороги visual не менялись.
