# Preview workflow verification

This disposable pull request verifies that the pull request author can create a
separate Vercel Preview by posting `/preview` in the pull request comments.

The pull request is not intended to be merged.
