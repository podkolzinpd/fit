# Fit — текущее состояние проекта

> Rolling snapshot для продолжения между сессиями. Максимум 120 строк.
> После каждого подтверждённого merge заменяйте сведения ниже, не добавляйте
> хронологию: полная история уже хранится в Git и Tracker.

Обновлено: 2026-08-09
Проверенный `main`: `79559f6` (`fix(db): minimize public grants (#351)`)

## Активная работа

- `YAFIT-244`: добавить корневое восстановление после render error без
  дублирования штатных AsyncView-состояний.

## Последняя проверенная продуктовая точка

- `#341` / `YAFIT-232`: карточка клиента у тренера приведена к общей сетке
  Fit без изменения логики данных.
- `#342` / `YAFIT-233`: тренер видит историю тренировок, созданных клиентом,
  и может отправить ему план.
- `#343` / `YAFIT-236`: кэш запросов очищается при смене авторизованного
  пользователя, поэтому данные одной сессии не видны в другой.
- `#344` / `YAFIT-238`: у клиента может быть только одна активная тренировка;
  повторный старт открывает уже идущую. Инвариант защищён и UI, и БД.
- `#345` / `YAFIT-239`: завершённая тренировка может ссылаться только на этап
  цели своего клиента; чужой этап серверно отклоняется.
- `#346` / `YAFIT-241`: root и подключённый тренер видят разрешённую
  AI-сводку клиента; prompt, модель и содержание не менялись.
- `#347` / `YAFIT-243`: Join flow выполняет один claim для ссылки; повтор
  возможен только по явной кнопке.
- `#348` / `YAFIT-266`: копирование завершённой тренировки клиента создаёт
  заполняемый план тренера; фактические секунды не блокируют сохранение,
  а клиент видит отправленный ему план.
- `#349` / `YAFIT-231`: GitHub Actions запускает Supabase без Mailpit —
  неиспользуемого почтового интерфейса с конфликтующим фиксированным портом.
- `#350` / `YAFIT-237`: React Router обновлён до patched 7.18.2; маршруты,
  guards и пользовательское поведение не менялись.
- `#351` / `YAFIT-240`: у браузерных ролей убраны лишние table grants и
  `PUBLIC EXECUTE`; явные права `authenticated` на штатные RPC сохранены.

## Проверки

- `#348`: целевые unit — 96 passed; мобильный Chromium e2e полного пути
  тренер → клиент прошёл; WebKit 390 px проверен; `npm run check` — 367
  тестов, lint, typecheck, coverage, DB types, iOS permissions и build.
- `#349`: локальный Supabase через Podman запускается с `--exclude mailpit`;
  чистый reset БД; `db:test` — 43 файла, 401 SQL/RLS-проверка; DB types
  проверены. Изменения приложения, миграций и iOS bundle не требовались.
- `#350`: `npm run check` — 367 тестов, lint, typecheck, coverage, DB types,
  iOS permissions и build; production audit не нашёл уязвимостей. Свежий
  iOS bundle из `main` собран, установлен и запущен на iPhone 17 без открытия
  дополнительного окна Xcode.
- `#351`: чистый reset БД; `db:test` — 44 файла, 406 SQL/RLS-проверок;
  мобильный Chromium auth e2e — 4 passed; `npm run check` — 367 тестов.
  Свежий iOS bundle из `main` собран, установлен и запущен на iPhone 17 без
  открытия дополнительного окна Xcode.

## Ближайшее действие

1. Завершить `YAFIT-244` (recover после render error).
2. Затем: `YAFIT-246` (изолированные e2e fixtures и WebKit CI).
3. `YAFIT-245` не начинать без отдельного разрешения пользователя и
   предварительного описания механики и рисков.

## Постоянные ограничения

- LLM-разбор — основная функция продукта; UI-правки не меняют prompt,
  matching, fallback и сохранение без отдельного решения.
- UI перед сдачей проверяется в WebKit на ширине 390 px.
- После merge изменений приложения обновляется и заново запускается iOS bundle.
- Локальные Supabase-проверки работают через установленный Podman; Docker не
  требуется.
- Текущий backlog и статусы задач подтверждаются через YAFIT; этот файл служит
  индексом, а не заменой Tracker.
