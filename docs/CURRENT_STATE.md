# Fit — текущее состояние проекта

> Rolling snapshot для продолжения между сессиями. Максимум 120 строк.
> После подтверждённого merge сведения заменяются, а не накапливаются:
> полная история хранится в Git, PR и Tracker.

Обновлено: 2026-08-21
Проверенный базовый `main`: `6dcc3e8` (`YAFIT-332: действия с прошлой тренировкой (#502)`)

## Последнее продуктовое изменение

- `YAFIT-290` — рабочая очередь внимания основного тренера.
- Клиент может отдельно задать вопрос по завершённой тренировке; обычный факт,
  комментарий или RPE не создаёт обязательной задачи тренеру.
- На главной тренера показывается не больше одного действия на клиента:
  вопрос, затем дискомфорт, затем нейтральная необходимость следующего плана.
- Ответ тренера закрывает вопрос автоматически; также доступно явное действие
  «Вопрос решён». Планирование можно отложить на две недели.
- Несколько тренеров и выбор адресата вопроса оставлены на следующий этап.
- Следующая задача без отдельного подтверждения не начинается.

## Последняя проверенная продуктовая точка

- Главные страницы обеих ролей сохраняют voice-first действие и ввод текстом.
  Клиентская главная показывает ближайшее назначение, состоявшуюся неделю и не
  более одного вторичного акцента: ответ тренера, рекорд или цель.
- Создание тренировки использует единый ручной каталог направлений «Силовая» и
  «Бег», компактные фильтры, недавние упражнения и множественный выбор.
- Голосовой/текстовый разбор понимает порядок `3 по 15 20 кг` и
  `20 кг 3 по 15`, устойчив к части опечаток и сохраняет безопасный локальный
  результат при временной ошибке LLM.
- Сохранённые тренировки используют компактную хронику упражнений с раскрытием
  подходов и отдельной кнопкой истории; копирование и удаление находятся в меню.
- Trainer и Client Progress используют одну читаемую ИИ-сводку с реальным
  горизонтом истории, русскими формулировками и максимум одним знаком после
  запятой. Обновление ИИ восстановлено в `#483/#485`.
- Ручной беговой MVP завершён: обычный бег, интервалы, активное восстановление,
  СБУ, RPE и компактная динамика работают в планировании, Live и истории.
- Системный каталог содержит локальные public-domain пары кадров упражнений;
  внешнего медиасервиса и лицензионной зависимости нет.

## Инфраструктура

- Локальная разработка и проверки работают только через Podman. Docker у
  владельца проекта не установлен и не используется.
- Yandex Cloud stage содержит Managed PostgreSQL 17 и Serverless Containers;
  миграции доставляются автоматически через GitHub OIDC, private runner и
  forward-only policy.
- Read-only Yandex ID pilot и `/v1/clients` работают на stage. Production
  frontend и основной tenant пока остаются на Supabase; полный cutover не
  выполнен.
- Новая cost-sensitive, destructive или identity-инфраструктура требует
  отдельного решения и не добавляется в продуктовые UI-задачи.

## Проверки YAFIT-331

- Component-контракт главной зелёный; полный `npm run check` зелёный: 608
  frontend tests, coverage, lint, TypeScript, API, infra policy и production
  build.
- Реальный сценарий самостоятельного плана на завтра пройден в WebKit и
  визуально принят на 390/430 px; эталоны зафиксированы в репозитории.
- CI обязателен до merge.

## Проверки YAFIT-332

- Прошлые планы вынесены в отдельную нейтральную очередь для клиента и
  тренера; на главной клиента показывается не больше одного такого плана.
- Одно действие «Выбрать действие» открывает запись результата, перенос или
  решение «Не состоялась»; сохранённый план можно вернуть на новую дату.
- `cancelled` не участвует в выполненных тренировках, фактах, рекордах и
  тоннаже, но сохраняет назначение и учитывается в регулярности.
- Права и optimistic locking проверены SQL-матрицей; голосовой ввод, Live,
  SpeechKit и ИИ-анализ не менялись.

## Проверки YAFIT-290

- RPC и RLS-матрица проверяют основной сценарий, повтор вопроса, конфликт
  версий, посторонний доступ, отложенное планирование и старые client-профили
  без membership-строки.
- Мобильный WebKit-сценарий проходит полный цикл: клиент задаёт вопрос,
  основной тренер видит его в очереди и отвечает, после чего вопрос исчезает
  из очереди, а клиент видит ответ.
- Полный `npm run check` зелёный: 616 frontend tests, coverage, lint,
  TypeScript, типы БД, iOS permissions, infra policy, API и production build;
  отдельно зелёные 559 SQL-проверок и мобильный WebKit E2E.
- Главные голосовые действия обеих ролей, Live, LLM и SpeechKit не менялись.

## Ближайший продуктовый порядок

- Полный бэклог ценности: `docs/design/PRODUCT_VALUE_BACKLOG_2026-08-21.md`.
- После завершения `YAFIT-290` остановиться. Следующая задача выбирается и
  подробно согласуется отдельно. Аналитика основного цикла перенесена в конец
  программы, когда появится достаточный пользовательский поток.

## Отложено

- `YAFIT-245` (устойчивость AI-разбора) не начинать без отдельного разрешения и
  предварительного описания механики и рисков.
- `YAFIT-234` (защита SpeechKit relay) отложен; голосовой путь не менять.
- `YAFIT-235` — Webvisor сохранён для исследовательских метрик.
- Новые виды спорта, питание, социальные функции, внешние носимые устройства и
  дополнительные ИИ-блоки не брать до завершения P0/P1 и пилота.
