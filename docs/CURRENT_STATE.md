# Fit — текущее состояние проекта

> Rolling snapshot для продолжения между сессиями. Максимум 120 строк.
> После каждого подтверждённого merge заменяйте сведения ниже, не добавляйте
> хронологию: полная история уже хранится в Git и Tracker.

Обновлено: 2026-08-08
Проверенный `main`: `0c4172a` (`feat: unify the client shell (#328)`)

## Активная работа

- `YAFIT-221`: voice-first главный экран клиента на общем проверенном пути
  разбора и сохранения тренировки.

## Последняя проверенная продуктовая точка

- `#307`: основной сценарий тренера и review защищены WebKit-smoke на
  390/375/360 px; LLM/SpeechKit не менялись.
- `#308`–`#313`: live-экран уплотнён, RPE, выбор клиента, таблицы подходов,
  заголовки и история упражнений приведены к общим компонентам.
- `#314`: на «Сегодня» добавлены понятные состояния ошибки разбора и retry;
  текст остаётся в композере, LLM/SpeechKit не менялись.
- `#315`: тренер может добавить или изменить отзыв к завершённой тренировке;
  клиент видит его только для чтения.
- `#316`: реальный iPhone WebKit закрепляет 390/375/360 px и live-сценарии
  отдыха, «Сет» и «Круговой».
- `#317`: добавлены компактные правила поставки: preflight, полные e2e-fixtures,
  последовательный mutating e2e и безопасное создание PR.
- `#318`: в карточках и деталях тренировки добавлены производные статусы
  «Частично» и «Пропущена», без расширения модели БД.
- `#319`: в `docs/design` зафиксировано, какие пункты UX/UI-аудита реализованы
  и какие сознательно остались вне scope.
- `#320`: отдельная trainer-вкладка «Аналитика» удалена; прогресс доступен из
  карточки клиента, клиентские маршруты прогресса сохранены.
- `#321` / `YAFIT-214`: три trainer-вкладки выровнены; отдельная карточка
  восстановленного черновика удалена, тихое восстановление сохранено.
- `#322` / `YAFIT-215`: `/today` стал voice-first; существующие SpeechKit,
  LLM-разбор, review и save flow подключены без изменения их контрактов.
- `#323` / `YAFIT-216`: голосовое действие снова доступно при восстановленном
  draft; нативная сборка ошибочно переключена на локальный Supabase.
- `#324` / `YAFIT-217`: стандартная нативная сборка снова использует рабочий
  release-parity контур; локальный Supabase оставлен отдельным явным режимом.
- `#325` / `YAFIT-218`: ручной выбор показывает последние упражнения клиента,
  затем недавние тренера и остальной каталог без дублей.
- `#326` / `YAFIT-219`: имя тренера сохраняется в auth-профиле, имя клиентской
  карточки — в canonical client; после merge обнаружено, что клиентский tab
  ошибочно открывает trainer-only форму вместо существующего client profile.
- `#327` / `YAFIT-219`: client profile подключён к `/me/profile`; trainer и
  client routes закрыты с обеих сторон; onboarding ждёт refresh до сообщения
  об успехе.
- `#328` / `YAFIT-220`: клиентская оболочка сведена к одной feature, клиент
  видит автора назначения и открывает только собственную историю упражнения;
  LLM/SpeechKit, БД и RLS не менялись.

## Ближайшее действие

1. Завершить YAFIT-221: voice-first главный экран клиента без отдельного
   парсера и trainer-only действий.
2. Затем YAFIT-222: явно показать, какие данные клиента доступны тренеру и
   какие изменения он получает.

## Проверки

- Локальная БД после `main` чисто пересоздана через Podman.
- `#316` влита: CI прошёл для app, database и e2e; WebKit live-сценарии
  проверены последовательно на 390 px.
- `#318` до merge: прошли `db:reset`, unit-тесты статусов, trainer e2e,
  iPhone WebKit 390 px и полный `npm run check` (349 тестов).
- `#321`: CI app, database, e2e и Vercel зелёные; post-merge bundle из
  `9642ae3` запущен в iPhone 17 Simulator, Xcode открыт.
- `YAFIT-216` до PR: Today E2E 6/6; WebKit 390/375/360 и draft-in-viewport 4/4;
  `npm run check` — 350 тестов, lint/typecheck/db types/build; локальный iOS
  bundle синхронизирован через Podman-конфигурацию, собран и запущен на iPhone
  17 Simulator, Xcode открыт.
- `YAFIT-217`: причина регрессии подтверждена отсутствием LLM-настроек в
  локальной Edge Function; LLM-код не менялся. Прошли целевой voice E2E,
  WebKit 390 px, `npm run check` (352 теста) и Xcode build; release-parity bundle
  без локального Supabase запущен на iPhone 17 Simulator, Xcode открыт.
- `YAFIT-218`: до PR прошли unit-тесты порядка и дедупликации, полный trainer
  flow, WebKit 390 px и `npm run check` (353 теста); PR #325 влит в main.
- `YAFIT-219` / `#327`: CI прошёл; post-merge bundle из `989aaa1` собран,
  установлен и запущен на iPhone 17 Simulator, Xcode открыт.
- `YAFIT-220` / `#328`: CI прошёл; post-merge bundle из `0c4172a` собран,
  установлен и запущен на iPhone 17 Simulator, Xcode открыт.
- Проверки следующего продуктового кода фиксируются только после фактического
  запуска, без переноса старых зелёных результатов на новый commit.

## Постоянные ограничения

- LLM-разбор — основная функция продукта; UI-правки не меняют его prompt,
  matching, fallback и сохранение без отдельного решения.
- UI перед сдачей проверяется в WebKit на ширине 390 px.
- После merge изменений приложения обновляется и заново запускается iOS bundle.
- Локальные Supabase-проверки работают через установленный Podman; Docker не
  требуется.
- Текущий backlog и статусы задач подтверждаются через YAFIT; этот файл служит
  индексом, а не заменой Tracker.
