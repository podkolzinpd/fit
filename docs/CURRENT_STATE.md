# Fit — текущее состояние проекта
> Rolling snapshot для продолжения между сессиями, максимум 120 строк. После merge сведения заменяются; полная история хранится в Git, PR и Tracker.
Обновлено: 2026-09-10. База реализации: `main` `d642f91e` (#859). YAFIT-496–498 выпущены: фактический вес переносится в следующие нетронутые подходы той же плановой группы, обещание об отзыве и вопрос тренеру показываются только при текущем активном membership, а узкая тяга получила точное отдельное медиа. YAFIT-340 реализует одно напоминание через 20 минут реального бездействия в незавершённой Live-тренировке. Постоянное разрешение на автономный выпуск зафиксировано в межсессионной памяти.
## Последняя проверенная продуктовая точка
- YAFIT-480: Home, Progress и завершение используют общий подтверждённый личный факт без ожидания ИИ: база/изменение/рекорд, две записи и даты. После save/edit/delete обновляются производные запросы. Старая сводка подписана отдельно. Первая запись больше не PR в обоих backend. YAFIT-481 добавляет компактную карту последней тренировки на Home; YAFIT-482/483 добавляют сводку и центр результатов.
- Главные страницы обеих ролей сохраняют voice-first действие и ввод текстом; Client Home показывает ближайшее назначение, состоявшуюся неделю и максимум один вторичный акцент. Live разделяет нейтральный таймер и активный отдых.
- Создание, Live и завершение прошлого плана сохраняют прежнюю логику без дубликатов. Yandex stage поддерживает non-Live lifecycle, cancel/reschedule, комментарий и author-scoped soft-delete с optimistic/tenant checks.
- Единый мобильный viewport-контракт восстанавливает полную высоту Trainer, Client и авторизации после закрытия клавиатуры даже при resize WebKit.
  Создание тренировки — каталог «Силовая»/«Бег», недавние упражнения, фильтры.
  Строки мобильного каталога не сжимаются и прокручиваются внутри sheet; новый
  клиент из пустых «Тренировок» и «Прогресса» попадает в форму своего профиля.
- Голосовой/текстовый разбор понимает числа словами, дробный вес, интервальный
  бег и связки; неоднозначность открывает проверку. Сохранённые тренировки —
  компактная хроника с копированием и удалением в меню.
- ИИ-сводка и production-разбор — через Yandex Cloud Functions, локальный разбор
  — в Supabase. `main` содержит Yandex PostgreSQL operations для `app_feedback`;
  legacy DataLens/Telegram и новые внешние DataLens/Telegram/Tracker подключения отложены.
- План нового анализа: `docs/design/PROGRESS_AI_WHOLE_PERIOD_ANALYSIS.md`.
  Контракт v8 получает все упражнения, сессии и plan/fact-подходы выбранного и
  предыдущего периодов, цель, структурированную обратную связь и замеры без
  закрытых заметок; большой вход анализируется частями без молчаливой потери.
  Клиентский ответ строится как «Движение к цели» → «Что заметил ИИ» → «Что
  делать дальше» → «Чего не хватает», а числовой пересказ без вывода
  отклоняется до сохранения.
- Публичные условия, политика конфиденциальности и страница запроса удаления аккаунта доступны без таббара. Принятая редакция сохраняется под RLS; заявка на удаление отменяема и не удаляет данные автоматически. Интерфейс входа и регистрации больше не предлагает Google; существующие Google-аккаунты и серверный провайдер не удалены.
- Assistant открыт всем тренерским аккаунтам и защищён `TrainerOnly`; `VITE_ASSISTANT_NAV_ENABLED=false` остаётся общим kill switch. Durable turns/actions и narrow
  RPC сохраняют owner/RLS. Новые turn'ы создают workout draft, прежние карточки
  остаются читаемыми; исходная диктовка сохраняется при уточнении клиента.
  Числовые поля черновика допускают полную очистку перед вводом нового значения;
  многострочный composer держит микрофон и отправку на одной нижней линии, а
  неизвестное имя открывает полный выбор прямых и подключённых клиентов.
- PWA показывает отдельный путь установки для iPhone через Safari и для Android
  через системное окно установки либо меню Chrome. Беговой MVP и локальный каталог работают; Yandex Web Push producer,
  lease/retry и минутный private timer работают на stage; гребля использует темп `/500 м`.
- Каталог сохраняет 663 прежних ref и добавляет 317 проверенных Gym Pro
  animation family с локальными роликами; после true duplicate основной выбор
  содержит 312 карточек. Старые id, варианты, синонимы и custom-карточки
  доступны в поиске/истории. Сборка сохраняет video URL всех новых Gym Pro:
  строки статичны до тапа, играет только последняя нажатая, повторный тап
  открывает технику, а `+ / ✓` независимо меняет выбор. Каталог и история
  показывают медиа только при наличии точной Vital-анимации или отдельной
  проверенной пары reference-кадров; совместимые
  исторические дубли наследуют анимацию канонической карточки без смены `ref`.
  Все 1 240 прежних `base-*`/`fedb-*` JPG удалены из поставки, а их URL
  блокируются на уровне компонента. Узкая тяга верхнего блока использует
  точную двухкадровую технику и не наследует wide-grip ролик. Непокрытое упражнение остаётся текстовым;
  при загрузке или ошибке приватного Vital-медиа старое фото не используется.
- Client и Trainer Progress используют одну короткую историю подтверждённого
  периода: лучший результат, тренировки, `X/Y` недель, улучшенные упражнения,
  карта тела, сравнение, связь с целью и ближайший план. Тренер видит plan/fact,
  сигналы и публикацию; выполнение плана не выдаётся за прогресс к цели.
- Trainer Progress сохраняет прежнюю композицию. У клиента сверху единый период с превью ИИ, затем неделя и результаты,
  полная карта ниже замеров и свёрнута. С Home она открывает точную
  тренировку, зону и даты с переходом к источнику и возвратом в тот же срез. Измерения, карта тела и LLM-анализ используют только подтверждённые метрики;
  неподтверждённый текст заменяется deterministic fallback. Query-действия не
  сбрасывают прокрутку, ошибки замеров локальны, бег следует общему периоду.
- Карта Home занимает полную ширину карточки: фигура по центру под переключателем, группы выровнены по верху фигуры. Изменение результата показано сразу с датой прошлой записи; переходы — «Открыть тренировку» и «Разбор нагрузки». Общий счётчик включает кардио; остальные группы раскрываются. План: `docs/design/CLIENT_HOME_RESULT_ALIGNMENT.md`.
- Карточка спортсмена у тренера — один нейтральный вход в планирование,
  равноправные переходы в историю/прогресс, действие «Добавить этапы». Выбор
  фигуры/схемы — настройка аккаунта тренера, не смешивается с выбором клиента.
- Расписание показывает месяц, неделю, события и планы без времени; Assistant сохраняет тренировку только после подтверждения черновика.
- После успешного завершения клиент видит одноразовый отчёт: краткие факты,
  точный план/факт, один главный результат, обратную связь, мышцы и действия.
  История и тренерский сценарий сохраняют компактный экран YAFIT-280/341.
- Live сохраняет каждое изменение подхода на устройстве, автоматически досылает pending-черновики и ограничивает зависшие запросы. Finish сначала отправляет
  открытую строку; ошибка закреплена у повтора. Поля и галочка сохраняют зону
  нажатия 44 px; соседние строки стоят вплотную и каждая занимает 44 px.
  Активное упражнение не обёрнуто во вторую округлую карточку, а план
  раскрывается по требованию без изменения сохранённых плановых данных.
  Подтверждённый фактический вес подставляется дальше внутри той же плановой
  группы; ручной факт важнее, будущие подходы и сам план заранее не сохраняются.
  После 20 минут без содержательного действия клиент получает одно мягкое
  напоминание с возвратом в Live и выбором продолжить или завершить. Работающий
  отдых продлевает окно; завершение, удаление и отключение настройки снимают
  уведомление. В iOS доставку планирует ОС, в вебе есть service-worker и inline
  fallback при следующем открытии.
- Клиент может безопасно отключить текущего тренера в профиле: серверная
  операция атомарна, повтор идемпотентен, самостоятельная история, назначения,
  замеры и цели сохраняются. Серверное переподключение после явного отключения
  использует тот же атомарный merge и не допускает двух активных тренеров.
- Новые одноразовые коды приглашения в клиентском профиле, карточке клиента у
  тренера и первом плане имеют единый compact copy-action, подтверждение
  «Скопировано» и fallback для iOS/WebView.
## Инфраструктура и Yandex Cloud
- Локальная разработка и проверки используют только Podman. Docker не нужен.
- Stage содержит Managed PostgreSQL 17 и Serverless Containers. Миграции
  доставляются автоматически через GitHub OIDC, private runner и forward-only
  policy; `fit_api` не имеет прямых domain grants, а private push timer создаётся
  только после отдельного cost approval и health-check точной ревизии.
- Ограниченный Yandex ID pilot и доменная цепочка представлены в `000001–000027`:
  `000026` добавляет actor-scoped Assistant state, `000027` — безопасное linking
  FIT-профиля с Yandex ID и read-write app-session только через явный
  `yandex`/`read_write` rollout assignment. Assistant, session digests и push
  secrets не выставляются через `ops_readonly`.
- Live AI smoke: parser прошёл, summary получил 400 из-за формата тестового запроса; исправление и повтор описаны в `docs/design/CLIENT_PROGRESS_AI_VERIFICATION.md`. Native AI использует metadata IAM token без статического ключа; точную роль
  один раз выдаёт `fit-stage-api` администратор, а OIDC не меняет folder IAM.
- Yandex OAuth использует PKCE и публичный Client ID; secret browser-контракту
  не нужен, Supabase-сессия при пилотном входе не создаётся.
- `main` содержит 14-дневную read-write Yandex ID-сессию: stage валидирует/отзывает opaque token, frontend восстанавливает профиль и блокирует истёкшую/неразрешённую сессию.
- `main` расширяет единый Yandex API-контракт на read-write app-session:
  клиенты, связи, замеры/цели, тренировки, feedback и push state принимают
  `x-fit-session`; одновременная отправка read-only и read-write credentials
  закрывается с `401`. После безопасной привязки сервер сразу возвращает
  app-session только профилю с действующим `yandex`/`read_write` assignment;
  сама привязка сохраняется и для профиля, ещё не включённого в rollout.
- `main` содержит независимый default-off sticky route основного Trainer
  Assistant для одного перенесённого профиля. История, turns/actions,
  упражнения, parser и training summaries используют одну `x-fit-session` и
  Yandex API; mismatch или ошибка не переключают отдельный запрос на Supabase.
  Остальные пользователи и вкладки сохраняют текущий Supabase backend.
- `main` содержит отдельный default-off sticky route всего основного
  интерфейса для ровно одного перенесённого profile UUID. Профиль, клиенты,
  упражнения, цели/прогресс, тренировки/Live/post-workout, приглашения,
  Assistant, сводки, feedback и push используют один `x-fit-session`; ошибки
  не переключают отдельные запросы на Supabase. После выбора Yandex backend
  старая browser Supabase-сессия завершается. Вне allowlist production не
  меняется; UI и маршруты остаются прежними.
- Yandex migration `000035` сохраняет author-scoped пользовательские упражнения; доступ и `created_by` остаются tenant-safe.
- Yandex migration `000038` приводит push к multi-device контракту Supabase: отдельный PK подписки, `(user_id, endpoint)`, адресная строка outbox и удаление только протухшего устройства. API принимает endpoint только в actor-authenticated body; tenant catalog переносит подписки по `id`.
- `main` содержит `000036`: receipts/retry Telegram/Tracker и четыре analytics
  views. В кластере сохранён уже включённый managed access для DataLens, но
  отдельный пользователь, подключение и перенос дашбордов пока не выполнялись.
- Стабильный Vercel Preview синхронизируется с каждым verified `main`; callback,
  CORS и история не меняются, прочие ветки исключены из Git deployments.
- Read-only callback остаётся диагностическим экраном; полноценная app-session
  использует тот же основной UI и read-write stage API без изменения rollout
  для пользователей вне allowlist.
- Реальный invite → join → leave/remove smoke — внешняя проверка. TLS source проверен; до rollout остаются dry-run, freeze/rollback-окно и один tenant.
- Remote rehearsal умеет без раскрытия UUID выбрать самый маленький допустимый
  cohort для `audit`/`dry-run`; dry-run пропускает checksum-конфликты уже
  существующих stage tenants (до 100 для текущего малого источника), прочие
  ошибки не скрывает. Автовыбор для `apply` запрещён.
- В main вошёл Live #835 / 0574750: отдельный таймер, стабильные подходы и отдельная заметка клиента; изменения сохранены при объединении.
## Проверки последнего выпуска
- YAFIT-488: SQL/RLS проверяют версии согласий и изоляцию заявок. Публичные
  документы описывают фактические метрики, push, аналитику и Yandex Cloud AI;
  prompt, matching, fallback, сохранение сводок и SpeechKit не менялись.
- Полный check: frontend 1322, API 364 (+30 skipped), infra 112; lint/typecheck/build и проверки DB types/iOS permissions пройдены. Все 1014 SQL/RLS-тестов успешны. Auth и legal e2e прошли в Chromium и iPhone WebKit; Linux-эталон регистрации 390 обновлён по проверенному CI-снимку, visual 430/1440 уложился в допуск.
- PR #841 слит в `main`; production DB и Vercel deploy успешны. Публичные legal-маршруты, вход и регистрация проверены на `fit-drab.vercel.app`. Свежий main собран, чисто установлен и запущен на iPhone 17 Simulator с production Supabase-конфигурацией; Android проверен как PWA в мобильном Chromium.
- iOS production sync теперь до сборки проверяет обязательные HTTPS-настройки Supabase и не позволяет получить успешную чёрную сборку из-за отсутствующего env. Публичные условия и политика имеют собственную сенсорную прокрутку внутри мобильной оболочки.
- Remote audit `34229105217` выбрал cohort: 1 клиент, 5 строк в 28 таблицах. Dry-run `34244770314` безопасно откатил 10 stale stage cohorts; configured cohort остаётся пустым.
## Ближайший порядок
1. Перед отправкой в App Store/Google Play подставить утверждённые реквизиты оператора и публичный контакт; заявки на удаление обрабатывать по операционному регламенту. Для нативного iOS отдельно перевести Google OAuth из встроенного WebView в системный браузер.
Параллельная миграция Yandex cohort ведётся отдельно, её ветки в этот план не входят.
## Отложено
- DataLens и доставка `app_feedback` в Telegram/Tracker отложены до отдельного решения.
- `YAFIT-333/334` отложены; `YAFIT-335/337` завершены. `YAFIT-245` не начинать без решения; `YAFIT-234` отложен; `YAFIT-235` — Webvisor. Новые виды спорта, питание, social/wearables и ИИ-блоки — после P0/P1 и пилота.
