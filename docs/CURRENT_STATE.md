# Fit — текущее состояние проекта

> Rolling snapshot для продолжения между сессиями. Максимум 120 строк.
> После каждого подтверждённого merge заменяйте сведения ниже, не добавляйте
> хронологию: полная история уже хранится в Git и Tracker.

Обновлено: 2026-08-18
Проверенный `main`: `ca08f23` (`YAFIT-312: унифицировать типографику клиента и тренера (#427)`)

## Активная работа

- Yandex ID/profile vertical slice и серверная tenant-allowlist реализованы в
  коде. Следующий этап — зарегистрировать OAuth-приложение, отдельно одобрить
  deploy миграции `000004` и auth-revision, затем включить внутренний read-only
  pilot без переключения production frontend. PR `#419` с локальным guarded
  pilot открыт отдельно и не относится к продуктовой UI-фазе.
- Функциональный MVP признан достаточным для системной фазы удобства. Аудит
  `#420` зафиксировал P0/P1/P2; навигационная ясность завершена в `#421`,
  информационная архитектура Trainer Progress — в `#423`, visual regression
  ключевых маршрутов — в `#425`, типографический контракт — в `#427`. Следующая
  одна продуктовая задача — единая геометрия компонентов.

## Последняя проверенная продуктовая точка

- После `#427` display/title/section/body/caption/numeric образуют единый
  типографический контракт на Client Progress/Workouts/Live и Trainer
  Clients/Schedule/Progress. Экраны защищены на 390/430/1440 px, включая
  macOS/Linux-эталоны. Главные страницы, основные кнопки, LLM и SpeechKit не
  менялись.
- После `#423` первый экран Trainer Progress отвечает на три вопроса: какова
  регулярность клиента, где есть измеримый прогресс и что требует внимания.
  Подробный AI-анализ и замеры доступны отдельными уровнями; расчёты, LLM,
  SpeechKit и главные страницы обеих ролей не менялись.
- После `#421` «Кабинет» клиента, «Сегодня» и «Расписание» тренера имеют разные
  смысловые иконки; back/overflow/add и системные статусы используют общий SVG-
  контракт и зоны 44×44 px. Главные страницы обеих ролей, голосовая запись и
  ввод текстом остались без изменений.
- `#420` зафиксировал системный UX/UI-аудит после функционального MVP: текущий
  эталон — клиентская главная и тренировки; главные долги — Trainer Progress,
  покрытие сложных экранов, типографика/геометрия и desktop shell тренера.
- После `#417` локальный текстовый и голосовой ввод понимает обычный бег,
  короткие отрезки в метрах, время `мин:сек`, интервалы, активное восстановление
  и смешанный формат `400 м + 10 берпи`. Структура интервалов доезжает в форму;
  prompt/model/matching/fallback LLM и SpeechKit не менялись.
- После `#416` клиент и тренер видят компактный confirmed-only беговой прогресс:
  число пробежек, километраж, длительность, средний темп и RPE. Темп сравнивается
  только внутри одного формата на сопоставимой дистанции; максимальная дистанция
  не объявляется универсальным рекордом. Goal-aware LLM-сводка сохранена.
- После `#415` после выбора «Бег» тренер и спортсмен видят свободный, лёгкий,
  длительный, темповый, восстановительный бег, интервалы с пассивным/активным
  восстановлением, свою схему и отдельные СБУ. Все варианты используют единый
  `running` ref; смешанные тренировки остаются доступны через общий каталог.
- После `#398` Progress клиента собран вокруг goal-aware LLM-сводки: тренировочная
  сводка учитывает активную цель клиента, `TrainingSummaryCard` показывает статус
  цели и качество данных сводки.
- После `#395` любая завершённая тренировка считается состоявшейся, даже если
  план выполнен частично; Client Home отдельно и понятно показывает упражнение,
  точный подтверждённый результат и тип персонального рекорда.
- После `#393` voice-first остаётся первым действием Client Home: микрофон и
  ввод текстом сохранены над остальными блоками. Progress показывает главным
  показателем все завершённые тренировки за неделю или месяц, а назначенный
  план — отдельной расшифровкой. Форма замера свёрнута; LLM-сводка и периоды
  1/3/6 месяцев не менялись.
- Изолированный Yandex Cloud stage содержит приватные Managed PostgreSQL 17 и
  Serverless Container без прогретых экземпляров. Миграции `000001`–`000003`
  применены; API readiness подтверждён. Код `#413` добавил проверку Yandex ID,
  hashed identity mapping, allowlist и read-only `/v1/profile`, но миграция
  `000004` и новая revision ещё не развёрнуты. Production frontend продолжает
  использовать Supabase, публичный invoker и frontend cutover не включены.

## Последние проверки

- `#427` / `YAFIT-312`: merge `ca08f23` подтверждён; GitHub app/database/e2e,
  Vercel и iPhone WebKit прошли. Локально полный `npm run check` — 463 теста
  приложения и 20 API-тестов; visual regression — 8 passed. Production iOS
  bundle синхронизирован, собран, установлен и запущен на iPhone 17 Simulator;
  экран входа проверен контрольным снимком.
- `#423` / `YAFIT-310`: merge `e88d286` подтверждён; GitHub app/database/e2e,
  Vercel и iPhone WebKit прошли. Локально `npm run check` — 463 теста приложения
  и 20 API-тестов, lint/typecheck/build зелёные. Production iOS bundle
  синхронизирован, собран, установлен и запущен на iPhone 17 Simulator; экран
  входа проверен контрольным снимком.
- `#421` / `YAFIT-309`: merge `cd2a4a9` подтверждён; GitHub app/database/e2e,
  включая iPhone WebKit и visual 390/430/1440, прошли. Локально `npm run check`
  — 457 тестов приложения и 20 API-тестов, lint/typecheck/build зелёные.
  Production iOS bundle синхронизирован, собран, установлен и запущен на iPhone
  17 Simulator; экран входа проверен контрольным снимком.
## Ближайший roadmap

1. В auth-потоке отдельно завершить `#419`, OAuth-приложение Yandex ID и
   одобренный stage deploy; production frontend не переключать.
2. Унифицировать радиусы, высоты и отступы компонентов; desktop shell тренера
   делать отдельным этапом, не растягиванием мобильных карточек.

Ручной беговой MVP `YAFIT-300/301/307/302/303` завершён. Актуальный UX/UI-план
и отложенные фичи находятся в `docs/design/PRODUCT_USABILITY_AUDIT_2026-08-18.md`
и `docs/design/BACKLOG-OVERVIEW.md`.

## Отложенный backlog

- `YAFIT-245` (P0, устойчивость AI-разбора) **не начинать без отдельного
  разрешения пользователя и предварительного описания механики и рисков**.
- `YAFIT-234` (защита SpeechKit relay) отложен пользователем; голосовой путь
  не менять.
- `YAFIT-235` — Webvisor сознательно сохранён для исследовательских метрик.
- Новые аналитические блоки, интеграции и декоративную визуализацию не брать до
  завершения P0/P1 продуктовой полировки.

## Постоянные ограничения

- LLM-разбор и SpeechKit не менять без отдельного продуктового решения.
- UI перед сдачей проверяется на Client 390/430 px и Trainer 1440 px; для
  мобильных сценариев обязателен WebKit.
- После merge изменений приложения обновляется и заново запускается iOS bundle.
- Локальные Supabase-проверки требуют контейнерный runtime; когда он недоступен,
  обязательные migration/pgTAP-проверки выполняются в CI. Production-секреты не
  читаются и не выводятся.
- Backlog и статусы подтверждаются через YAFIT; этот файл — индекс, не замена
  Tracker.
